[About]
Version=1.0
Author=Dilma

[Type]
Class=DPElement
Sub=,Count|onEvent,,
Info=Вызывает событие по индексу в потоке
Tab=MultiThread

[Property]
Count=Задает число событий компонента|1|2

[Methods]
doEvent=Вызывает одно из событий onEvent в соответствии с его индексом (верхний индекс равен 0). Если индекс больше, чем кол-во точек onEvent, то ничего не происходит|1|
*Index=Номер (индекс) вызываемого события|4|1
*Data=Данные, выдаваемые в поток событиями onEvent|4|