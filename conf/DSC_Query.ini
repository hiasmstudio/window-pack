[About]
Version=1.10
Author=nesco
Mail=nesco_sochi@mail.ru

[Type]
Class=Element
Info=Запрос к источнику данных
Tab=DataBase

[Property]
+SQL=Содержит текст запроса|2|
DSManager=Менеджер источника данных|20|(empty)|DataSource

[Methods]
doQuery=Выполняет строку запроса на языке SQL|1|
onQuery=Событие происходит после выполнения запроса к источнику данных и выдает в поток полученные результаты ARG(Data)|2|
onColumns=Выдает в поток имена столбцов ARG(Fields)|2|
onError=Событие происходит в случае ошибки и выдает её описание в поток|2|
SQL=Текст запроса на языке SQL|4|2